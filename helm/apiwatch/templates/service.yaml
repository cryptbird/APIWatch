apiVersion: v1
kind: Service
metadata:
  name: {{ include "apiwatch.fullname" . }}
  labels:
    {{- include "apiwatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apiwatch.selectorLabels" . | nindent 4 }}
